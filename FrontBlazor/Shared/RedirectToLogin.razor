@inject NavigationManager Navigation

@code {
    protected override async Task OnInitializedAsync()
    {
        // Si ya estamos en la página de login, no redirigimos
        if (!Navigation.Uri.EndsWith("/login"))
        {
            Navigation.NavigateTo("/login");
        }
    }
}
